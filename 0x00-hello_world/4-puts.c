#include<stdio.h>
#include<string.h>

/**
 * main - program starts
 *
 * Return: 0 if program is successful
 */
int main(void)
{
	char str[52];

	strcpy(str, "\"Programming is like building a multilingual puzzle");

	puts(str);

	return (0);
}
